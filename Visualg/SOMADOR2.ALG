Algoritmo "Somador2"

Var
   num, soma, c: Inteiro

Inicio
   soma <- 0

   Para c <- 1 ate 5 faca
      Escreval("Digite um valor: ")
      Leia(num)
      soma <- soma + num
   FimPara

   Escreval("A soma dos valores é ", soma)

Fimalgoritmo